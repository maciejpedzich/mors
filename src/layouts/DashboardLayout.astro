---
import BaseLayout from './BaseLayout.astro';

export interface Props {
  title: string;
}

const { title } = Astro.props;
const { roundName } = Astro.locals;
---

<BaseLayout title={`${roundName} - ${title}`}>
  <section class="mx-auto pt-40 pb-14">
    <slot />
  </section>
</BaseLayout>
